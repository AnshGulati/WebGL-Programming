# WebGL Rendering Pipeline

1. Set up the canvas (on which rendering will be done).
2. Get the WebGL rendering context (To use WebGL API).
3. Specify the geometry (data).
4. Create the shader programs.
5. Create Buffer Objects and load data into them.
6. Link Shader Programs with the buffer objects.
7. Draw the Object (Render!).